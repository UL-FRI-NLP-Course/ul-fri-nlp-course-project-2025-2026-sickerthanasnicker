%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% FRI Data Science_report LaTeX Template
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[fleqn,moreauthors,10pt]{ds_report}
\usepackage[english]{babel}
\usepackage{url}

\graphicspath{{fig/}}

\JournalInfo{FRI Natural language processing course 2025/2026}
\Archive{Final report}
\PaperTitle{RAG assistant for Slovenian employment law}
\Authors{Tibor Koderman, Jerneja Krajcar, Blaž Mikec}
\affiliation{\textit{Advisors: Slavko Žitnik, Aleš Žagar}}
\Keywords{Slovenian employment law, legal question answering, retrieval-augmented generation, RAG, GaMS}
\newcommand{\keywordname}{Keywords}

\Abstract{
We built a retrieval-augmented generation assistant for Slovenian employment-law questions.
The final system intentionally uses one main approach, RAG, because the target machine is CPU-limited and legal correctness depends more on current official sources than on model memorization.
The assistant retrieves official legal passages, applies a strict Slovenian system prompt, cites retrieved sources, and refuses unsupported or out-of-domain questions.
We evaluated retrieval separately from generation, monitored official source availability, and expanded the corpus from a small curated slice to an official-plus-case-law knowledge base.
The current setup reaches 1.000 answerable Hit@3 with 0.000 false-evidence rate on a 40-question evaluation set, and offline RAG answers cite a matching retrieved source in all supported non-refusal cases.
}

\begin{document}
\flushbottom
\maketitle
\thispagestyle{empty}

\section*{Motivation}
General-purpose LLMs are risky for legal question answering because they may answer fluently while inventing legal rules, citations, or article numbers.
Our domain is therefore restricted to Slovenian employment law: employment contracts, termination, leave, wages, working time, sick leave, collective employment issues, and closely related statutory guidance.
The assistant is informational, not a substitute for a lawyer, and must refuse tax, inheritance, company-formation, foreign-law, or other non-employment-law questions.
This narrow domain also matches the course project goal for a domain-specific institutional assistant: define a strict use case, curate a source of truth, inject domain knowledge into an LLM, and evaluate whether the system knows when not to answer.

\section*{Related Work}
Existing Slovenian legal assistants such as Pravko and MojMaliPravnik show demand for accessible legal explanations, but they do not give us a reproducible course pipeline or a controlled official-source policy \cite{Pravko,MojMaliPravnik}.
For knowledge injection into LLMs we considered prompt engineering, RAG, fine-tuning, and tool use.
RAG is well suited to knowledge-intensive tasks because claims can be grounded in retrieved documents \cite{Lewis2020RAG}.
LoRA-style fine-tuning is attractive for style or task adaptation \cite{Hu2021LoRA}, while tool-using models can call external systems \cite{Schick2023Toolformer}; however, both add implementation and hardware cost.
Following the survey perspective that domain knowledge can be injected at several stages \cite{Song2025Injection}, we chose the simplest auditable path: source curation, retrieval, prompt guardrails, and evaluation before any fine-tuning.

\section*{Corpus and Sources}
We used COLESLAW 1.0 as the broad legal starting point \cite{COLESLAW}, but the final RAG corpus is source-prioritized rather than corpus-size-maximized.
The committed builder downloads current official sources and writes the reproducible corpus to \texttt{report/code/data/chunk.jsonl}.
The current corpus has 1,371 chunks: 1,059 PISRS article-level chunks, 189 official interpretation chunks, 93 official operational-guidance chunks, and 30 tertiary COLESLAW/sodnapraksa case-law chunks.
PISRS ingestion uses the public PISRS metadata endpoint and latest available NPB HTML document; official guidance is chunked by page/PDF/DOCX sections.

The final source policy is strict.
PISRS statutes, especially ZDR-1, are canonical primary law \cite{PISRSZDR1}.
GOV.SI, MDDSZ, ZZZS, IRSD, eUprava, SPOT, ESS, and OPSI are official interpretation or operational sources \cite{GovDelovnaRazmerja,GovMinimalnaPlaca,ZZZSBolniska,OPSIKolektivne}.
\textit{sodnapraksa.si} and selected COLESLAW case-law chunks are tertiary support only and may not override current statutes.
The source monitor currently checks 12 PISRS sources, 24 government or official interpretation/operational sources, and one official case-law index; the latest run found all reachable.
This directly addresses the data-curation requirement: datasets available elsewhere are linked, small transformed chunks and evaluation questions are committed, and transformation scripts for COLESLAW and optional PEFT data are included in the repository.

\section*{System}
The implemented pipeline is RAG-first.
Documents are split into chunks with metadata such as law name, article, title, source URL, source type, priority, retrieval timestamp, and validity status.
The current retriever is normalized BM25 with a simple lexical fallback, which keeps the project runnable on CPU and without a vector database.
The evaluation layer also reuses the project retriever so that retrieval scores correspond to the actual answer-time context.
At answer time the top retrieved chunks are formatted as context and passed to the generator with the selected strict RAG v3 prompt.
The retriever and prompt both enforce source priority: primary law, official interpretations, official operational guidance, then case law.
Case-law chunks are suppressed for statutory questions unless the question asks about practice, courts, or interpretation.
The prompt requires Slovenian-only answers, checks Slovenian employment-law scope, and uses a fixed refusal sentence when the context is insufficient.
It also tells the model how to handle short ambiguous questions such as ``Ali me lahko odpustijo?'' without giving a falsely complete legal conclusion.

The preferred model for the next live deployment is GaMS-1B-Chat, because it is Slovenian-focused and CPU-friendlier than larger GaMS variants \cite{GaMS1B}.
Until GaMS is served through Open WebUI or a compatible local endpoint, the repository keeps deterministic offline tests and Ollama/Open WebUI fallbacks.
The final Open WebUI option documented in the repository wraps the best currently runnable model with the selected strict prompt and deterministic settings.
Fine-tuning data preparation exists only as an exploratory artifact; it is not used for the final claim.
This is a deliberate trade-off: LoRA could teach style or answer format, but it would not solve source freshness, and a legal assistant should prefer retrieved current law over remembered parameters.

\section*{Evaluation}
We evaluate retrieval and answer quality separately.
The test set has 40 questions covering annual leave and regres, fixed-term contracts, termination, severance, sick leave/ZZZS, minimum wage 2026, working-time evidence, occupational safety, wage non-payment, collective agreements, case-law interpretation, ambiguous in-domain questions, and out-of-domain refusals.
Retrieval metrics are answerable Hit@3, false-evidence rate for unanswerable questions, and average context length.
Answer metrics are correctness, grounding, completeness, clarity, hallucination, supported-citation rate, and refusal accuracy.
The judge has an offline fallback so that the repository remains reproducible when no local model server is available.

\begin{table}[ht]
\caption{Final RAG optimization summary. Higher is better except false evidence, context length, and hallucination.}
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Configuration} & \textbf{Hit@3} & \textbf{False ev.} & \textbf{Ctx. words} \\
\midrule
\textbf{Official corpus} & \textbf{1.000} & \textbf{0.000} & \textbf{448.6} \\
Old curated slice & 1.000 & 0.000 & 199.2 \\
\bottomrule
\end{tabular}
\label{tab:retrieval}
\end{table}

\begin{table}[ht]
\caption{Offline answer evaluation on the expanded official corpus, judged by the deterministic offline judge.}
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Variant} & \textbf{Correctness} & \textbf{Hallucination} & \textbf{Citations} \\
\midrule
Baseline & 0.50 & 4.12 & 0.00 \\
\textbf{Strict RAG} & \textbf{2.95} & \textbf{1.68} & \textbf{1.00} \\
\bottomrule
\end{tabular}
\label{tab:answers}
\end{table}

\begin{table}[ht]
\caption{Regression checks for the three previously failing questions.}
\centering
\small
\begin{tabular}{lc}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
\texttt{q009}: fixed-term duration & ZDR-1 article 55 retrieved and cited first \\
\texttt{q012}: sick-pay payer & Current 30-day/31st-day ZDR-1/ZZZS rule retrieved \\
\texttt{q014}: ambiguous leave & Ambiguity warning plus ZDR-1 articles 159 and 161 \\
\bottomrule
\end{tabular}
\label{tab:manual}
\end{table}

The main result is that broader coverage must still be filtered by source authority.
The official corpus expands coverage substantially without allowing case law to dominate statutory answers.
The strict RAG setup improves correctness, grounding, hallucination, refusal behavior, and citation discipline over the no-context baseline.
The old q009, q012, and q014 failures are now covered by retrieval and offline generated answers; q012 was also updated to the current official 30-working-day rule rather than the older 20-day wording.
Remaining answer-quality limits mostly come from extractive sentence selection and the simple offline judge, not from missing official sources.

\section*{Reproducibility and Compliance}
The final submission requirements are covered by repository evidence rather than by the report alone.
The README gives setup and reproduction commands, the requirements file lists runtime dependencies, and the example environment file documents optional model endpoints without committing secrets.
The generated official chunks, evaluation questions, and result files make the current claims inspectable.
The scripts compile, JSON configuration files validate, retrieval can be regenerated offline, and the report builds with \texttt{latexmk}.

The grading guidelines ask for a final report with analyses and discussion, a fully reproducible repository, readable results, sensible measures, and clear limitations.
The main compliance choices are therefore explicit: one implemented solution is RAG; fine-tuning is marked exploratory; COLESLAW is linked and transformation scripts are included; official sources are monitored; retrieval and generation are evaluated separately; citation validation is tracked; and remaining limitations are documented instead of hidden.
Peer review is the only major course obligation outside this repository.

\section*{Limitations and Future Work}
The biggest technical limitation is still legal validation: the repository has deterministic checks and non-expert manual review, but no qualified legal-expert audit.
The next highest-impact RAG work is denser legal reranking, better answer synthesis over multiple retrieved articles, and expansion of the source monitor to deeper document freshness checks.
Dense embeddings and reranking are promising, but should be added only after the official corpus is clean and monitored.
Fine-tuning remains a future option if RAG and prompting stop improving grounded correctness.

\bibliographystyle{unsrt}
\bibliography{report}

\end{document}
